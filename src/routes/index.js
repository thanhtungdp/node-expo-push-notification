import registerTokenDevice from './registerTokenDevice';
import listTokenDevice from './listTokenDevice';
import pushNotification from './pushNotification';

export {
	registerTokenDevice,
	listTokenDevice,
	pushNotification
}

export default {
	registerTokenDevice,
	listTokenDevice,
	pushNotification
}